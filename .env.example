# STEMentora Frontend Environment Variables

# IMPORTANT: Vite uses .env files in this priority:
# 1. .env.local (highest priority - for local dev, git-ignored)
# 2. .env (for production/default)
# 3. .env.example (this template file)

# For Local Development:
# Create a .env.local file (it's git-ignored) with:
# VITE_API_BASE_URL=http://localhost:8000

# For Production (Vercel):
# The .env file contains:
# VITE_API_BASE_URL=https://api.stementorat.com
